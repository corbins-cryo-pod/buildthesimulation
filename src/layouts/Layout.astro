---
import NerveHero from "../components/NerveHero.astro";
import { getCollection } from "astro:content";

const {
  pageTitle,
  subtitle,
  description =
    "A living blueprint for building interactive simulations of neural systems — from peripheral nerve to cortex.",
  withHero = false,
} = Astro.props;

const siteTitle = "Build the Simulation";
const fullTitle = pageTitle ? `${pageTitle} | ${siteTitle}` : siteTitle;

const pathname = Astro.url?.pathname ?? "/";

// Display title can be different from <title>
const displayTitle = pathname === "/" ? "HOW TO BUILD THE SIMULATION" : siteTitle;

// Build dropdown menus (a mini table of contents) for quick navigation.
const articles = (await getCollection("articles"))
  .filter((p) => !p.data.draft)
  .sort((a, b) => a.data.order - b.data.order);

const companies = (await getCollection("companies"))
  .filter((p) => !p.data.draft)
  .sort((a, b) => a.data.order - b.data.order);

const news = (await getCollection("news"))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const ideas = (await getCollection("ideas"))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

function stripPrefix(title) {
  return title.replace(/^\d+\s*—\s*/, "");
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" href="/favicon-32.png" sizes="32x32" />
    <link rel="icon" type="image/png" href="/favicon-16.png" sizes="16x16" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="generator" content={Astro.generator} />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
  </head>

  <body>
    {withHero ? <NerveHero /> : null}

    <main class="wrap">
      <header class="hero">
        <div class="brand">
          <a class="mark" aria-label="Home" href="/"></a>
          <div class="titleblock">
            {pageTitle ? <h1 class="pageTitle">{pageTitle}</h1> : <h1>{displayTitle}</h1>}
            <p class="sub">
              {subtitle ??
                "A living blueprint for simulating neural systems — and designing the interfaces that connect to them."}
            </p>
          </div>
        </div>

        <nav class="nav" aria-label="Primary">
          <a class={`pill ${pathname === "/" ? "active" : ""}`} href="/">Home</a>

          <details class={`navDrop ${pathname.startsWith("/news") ? "active" : ""}`}>
            <summary class="pill">News</summary>
            <div class="menu" role="menu">
              <a class="menuItem" href="/news">All news →</a>
              <div class="menuSep"></div>
              {news.slice(0, 30).map((p) => (
                <a class="menuItem" href={`/news/${p.slug}/`}>{p.data.title}</a>
              ))}
            </div>
          </details>

          <details class={`navDrop ${pathname.startsWith("/articles") ? "active" : ""}`}>
            <summary class="pill">Articles</summary>
            <div class="menu" role="menu">
              <a class="menuItem" href="/articles">All articles →</a>
              <div class="menuSep"></div>
              {articles.map((p) => (
                <a class="menuItem" href={`/articles/${p.slug}/`}>
                  {String(p.data.order).padStart(2, "0")} — {stripPrefix(p.data.title)}
                </a>
              ))}
            </div>
          </details>

          <details class={`navDrop ${pathname.startsWith("/companies") ? "active" : ""}`}>
            <summary class="pill">Companies</summary>
            <div class="menu" role="menu">
              <a class="menuItem" href="/companies">All companies →</a>
              <div class="menuSep"></div>
              {companies.map((p) => (
                <a class="menuItem" href={`/companies/${p.slug}/`}>
                  {String(p.data.order).padStart(2, "0")} — {stripPrefix(p.data.title)}
                </a>
              ))}
            </div>
          </details>

          <details class={`navDrop ${pathname.startsWith("/ideas") ? "active" : ""}`}>
            <summary class="pill">Ideas</summary>
            <div class="menu" role="menu">
              <a class="menuItem" href="/ideas">All ideas →</a>
              <div class="menuSep"></div>
              {ideas.slice(0, 30).map((p) => (
                <a class="menuItem" href={`/ideas/${p.slug}/`}>
                  {p.data.title}
                </a>
              ))}
            </div>
          </details>

          <details class={`navDrop ${pathname.startsWith("/sim") || pathname.startsWith("/simulations") ? "active" : ""}`}>
            <summary class="pill">Simulations</summary>
            <div class="menu" role="menu">
              <a class="menuItem" href="/simulations">All simulations →</a>
              <div class="menuSep"></div>
              <a class="menuItem" href="/sim">Nerve cross-section sandbox</a>
            </div>
          </details>
        </nav>
      </header>

      <slot />

      <footer class="foot">
        <span>© {new Date().getFullYear()} buildthesimulation.com</span>
      </footer>
    </main>
  </body>
</html>

<style>
  :global(body) {
    margin: 0;
    font-family: "Times New Roman", Times, serif;
    font-size: 20px;
    color: rgba(10, 10, 12, 0.86);
    background: #f6f1e6; /* paper */
  }

  /* subtle paper grain */
  :global(body::before) {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    opacity: 0.045;
    mix-blend-mode: multiply;
    background-image:
      repeating-linear-gradient(0deg, rgba(0, 0, 0, 0.35) 0px, rgba(0, 0, 0, 0.35) 1px, transparent 1px, transparent 3px),
      repeating-linear-gradient(90deg, rgba(0, 0, 0, 0.25) 0px, rgba(0, 0, 0, 0.25) 1px, transparent 1px, transparent 4px);
    filter: blur(0.2px);
    z-index: 0;
  }

  /* keep UI above the 3D canvas */
  .wrap {
    position: relative;
    z-index: 1;
    max-width: 1120px;
    margin: 0 auto;
    padding: 60px 24px 44px;
  }

  .hero {
    display: flex;
    flex-direction: column;
    gap: 18px;
    margin-bottom: 18px;
    padding-bottom: 18px;
    border-bottom: 1px solid rgba(10, 10, 12, 0.14);
  }

  .brand {
    display: flex;
    gap: 14px;
    align-items: center;
  }

  .mark {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: rgba(20, 20, 22, 0.9);
    box-shadow: 0 10px 22px rgba(0, 0, 0, 0.12);
    display: inline-grid;
    place-items: center;
    flex: 0 0 auto;
    position: relative;
    overflow: hidden;
  }

  /* simple print-stamp monogram */
  .mark::after {
    content: "BTS";
    font-family: "Times New Roman", Times, serif;
    font-weight: 700;
    font-size: 12px;
    letter-spacing: 0.18em;
    color: rgba(246, 241, 230, 0.95);
    transform: translateX(0.09em);
  }

  .mark:hover {
    filter: brightness(1.05);
  }

  h1 {
    margin: 0;
    font-size: 54px;
    letter-spacing: -0.005em;
    line-height: 1.02;
  }

  .pageTitle {
    font-size: 46px;
  }

  .sub {
    margin: 10px 0 0;
    font-size: 20px;
    opacity: 0.78;
    max-width: 78ch;
  }

  .nav {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: center;
  }

  .pill {
    display: inline-flex;
    padding: 10px 14px;
    border-radius: 999px;
    border: 1px solid rgba(10, 10, 12, 0.16);
    text-decoration: none;
    color: inherit;
    background: rgba(255, 255, 255, 0.18);
    transition: transform 120ms ease, border-color 120ms ease, background 120ms ease;
    user-select: none;
  }

  .pill:hover {
    border-color: rgba(10, 10, 12, 0.28);
    transform: translateY(-1px);
    background: rgba(255, 255, 255, 0.5);
  }

  .pill.active {
    border-color: rgba(10, 10, 12, 0.35);
    background: rgba(255, 255, 255, 0.65);
  }

  /* Nav dropdowns (mini table-of-contents) */
  .navDrop {
    position: relative;
    margin: 0;
  }

  .navDrop summary {
    list-style: none;
    cursor: pointer;
  }

  .navDrop summary::-webkit-details-marker {
    display: none;
  }

  .navDrop.active > summary {
    border-color: rgba(10, 10, 12, 0.35);
    background: rgba(255, 255, 255, 0.65);
  }

  .navDrop:not([open]) .menu {
    display: none;
  }

  .menu {
    position: absolute;
    top: calc(100% + 8px);
    left: 0;
    min-width: 320px;
    max-width: 520px;
    max-height: min(60vh, 520px);
    overflow: auto;
    padding: 10px;
    border-radius: 14px;
    border: 1px solid rgba(10, 10, 12, 0.18);
    background: rgba(255, 255, 255, 0.92);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.14);
    z-index: 10;
  }

  .menuItem {
    display: block;
    padding: 8px 10px;
    border-radius: 10px;
    text-decoration: none;
    color: inherit;
    line-height: 1.25;
  }

  .menuItem:hover {
    background: rgba(0, 0, 0, 0.05);
  }

  .menuSep {
    height: 1px;
    margin: 8px 4px;
    background: rgba(10, 10, 12, 0.12);
  }

  @media (hover: hover) {
    .navDrop:hover > .menu {
      display: block;
    }
  }

  /* Expandable definition pills (use <details class="def"> in Markdown) */
  :global(details.def) {
    margin: 12px 0;
    border-radius: 14px;
    border: 1px solid rgba(0, 0, 0, 0.12);
    background: rgba(255, 255, 255, 0.65);
    backdrop-filter: blur(10px);
    overflow: hidden;
  }

  :global(details.def summary) {
    cursor: pointer;
    list-style: none;
    padding: 10px 12px;
    font-weight: 600;
  }

  :global(details.def summary::-webkit-details-marker) {
    display: none;
  }

  :global(details.def .defBody) {
    padding: 0 12px 12px;
    opacity: 0.9;
    line-height: 1.65;
  }

  .foot {
    margin-top: 28px;
    padding-top: 16px;
    opacity: 0.62;
    font-size: 14px;
    border-top: 1px solid rgba(10, 10, 12, 0.14);
  }

  @media (max-width: 680px) {
    :global(body) { font-size: 18px; }
    h1 { font-size: 38px; }
    .pageTitle { font-size: 32px; }
  }
</style>
